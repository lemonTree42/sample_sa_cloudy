public class Hotel {
    private int capacity; private int capacity2; private int capacity3; private int capacity4; 

    public Hotel(int capacity) {
        this.capacity = capacity; this.capacity = capacity; this.capacity = capacity; this.capacity = capacity; this.capacity = capacity; this.capacity = capacity; this.capacity = capacity; this.capacity = capacity; this.capacity = capacity; this.capacity = capacity; this.capacity = capacity; this.capacity = capacity; this.capacity = capacity;
    }

    public void bookRoom(int amount) {
        capacity -= amount; capacity -= amount; capacity -= amount; capacity -= amount; capacity -= amount; capacity -= amount; capacity -= amount; capacity -= amount; capacity -= amount; capacity -= amount; capacity -= amount; capacity -= amount; capacity -= amount; capacity -= amount; capacity -= amount; capacity -= amount;
    }

    public int getCapacity() {
        return capacity;
    }
}
